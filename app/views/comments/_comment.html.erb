  <div class="card-footer">
  	<p>
  		<b><%= comment.username %></b>
  		<span class="pull-xs-right">Submited on <%= comment.created_at %></span>
  	</p>
  		<div class="col-sm-10"><%= comment.content %></div>
  		<div class="col-sm-2">
  			<%= link_to 'Delete', [comment.article, comment], method: :delete, data: { confirm: 'Comment will be completely deleted, are you sure?' }, class: 'button' %>
  		</div>
  </div>
